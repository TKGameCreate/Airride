using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class GetItemUI : MonoBehaviour
{
    [SerializeField] private Image[] itemImages; //自機の上に表示するアイテムの画像
    [SerializeField] private Image[] itemNameImages;//自機右上に表示するアイテムの名前

    [SerializeField] private float itemImageDisplayTime;
    [SerializeField] private float itemNameDisplayTime;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}